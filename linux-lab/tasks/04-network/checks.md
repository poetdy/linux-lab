# Самопроверка

- Ты нашел лабораторный порт и можешь назвать его.
- На `127.0.0.1:18080` возвращается корректный HTTP-ответ.
- Ты понимаешь разницу между `refused` и `timeout`.

## Контрольные вопросы
- Что показывает `ss -tulpn`?
- Почему `ping` не заменяет проверку HTTP?
- С чего начать разбор сетевой проблемы на Linux-сервере?
