# Самопроверка

- `config.sample` ссылается на реальный лабораторный ключ.
- Каталог `.ssh` имеет права `700`.
- `authorized_keys` имеет права `600`.
- Ты можешь объяснить, почему эти права важны для SSH.

## Контрольные вопросы
- Что делает `authorized_keys`?
- Зачем нужен `Host`-алиас в `~/.ssh/config`?
- Почему мы тренируемся на копии, а не на реальном конфиге?
