# Самопроверка

- Лабораторный `sleep` можно найти и остановить штатно.
- После исправления `broken-service.sh` скрипт завершается с кодом 0.
- Ты понимаешь, почему `kill -9` не стоит использовать первым действием.

## Контрольные вопросы
- Чем `pgrep` удобнее `ps aux | grep`?
- В чем разница между процессом и службой?
- Когда нужен `systemctl status`, а когда достаточно `ps`?
