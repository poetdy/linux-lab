# Самопроверка

- `run-report.sh` запускается напрямую.
- `team.txt` имеет права `644`.
- В `restricted-dir` можно зайти и прочитать файл `inside.txt`.
- Ты можешь объяснить разницу между правами файла и каталога.

## Контрольные вопросы
- Зачем каталогу бит `x`?
- Почему `777` почти всегда плохая идея?
- Когда удобнее символическая запись `chmod`, а когда числовая?
