# Самопроверка

- В `errors-only.log` попали только строки с ошибками.
- В `failed-requests.log` нет успешных ответов `200`.
- Ты можешь показать последнюю ошибку одной командой.

## Контрольные вопросы
- Когда удобнее `tail -f`, а когда `grep`?
- Почему важно фильтровать по времени?
- Какие слова кроме `error` часто указывают на проблему?
